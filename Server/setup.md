1. Tạo Virtual Environment
Mở PowerShell:
python -m venv .venv
Kích hoạt:
.venv\Scripts\activate
Nếu thấy:
(.venv) PS C:\project>
là thành công.
2. Cài thư viện
pip install -r requirements.txt
Kiểm tra:
pip list